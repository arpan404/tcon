export const spec = { path: "toml_basic.toml", format: "toml" };
export const schema = t.object({
  app: t.object({
    host: t.string().default("0.0.0.0"),
    port: t.number().default(8080),
  }).strict(),
}).strict();
export const config = { app: { port: 3000 } };
