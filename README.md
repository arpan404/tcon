tcon - Typed Configuration
Built on Rust with a TS subset compiler, tcon allows you to define configuration types using TS + Zod Schema syntax for compile-time validation instead of runtime failures.

Note: This project is under active development. Check back frequently for updates.